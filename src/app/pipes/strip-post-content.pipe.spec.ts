import { StripPostContentPipe } from './strip-post-content.pipe';

describe('StripPostContentPipe', () => {
  it('create an instance', () => {
    const pipe = new StripPostContentPipe();
    expect(pipe).toBeTruthy();
  });
});
