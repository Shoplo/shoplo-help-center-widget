import { StripSpacePipe } from './strip-space.pipe';

describe('StripSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new StripSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
