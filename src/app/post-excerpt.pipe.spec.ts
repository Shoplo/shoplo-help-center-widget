import { PostExcerptPipe } from './post-excerpt.pipe';

describe('PostExcerptPipe', () => {
  it('create an instance', () => {
    const pipe = new PostExcerptPipe();
    expect(pipe).toBeTruthy();
  });
});
